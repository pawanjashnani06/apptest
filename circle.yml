machine:
  services:
    - docker

dependencies:
  override:
    - ./build.sh
    - docker info
    - docker build --rm=false -t greenbankstub .
    - curl -fsSL https://goss.rocks/install | GOSS_DST=./goss sh

deployment:
  dev:
    branch: master
    commands:
      - ./deploy.sh
